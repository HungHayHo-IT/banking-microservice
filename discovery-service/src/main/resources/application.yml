server:
  port: 8761

spring:
  application:
    name: "discovery-service"


eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
    shutdown:
      enabled: false
  health:
    readiness-state:
      enabled: true
    liveness-state:
      enabled: true